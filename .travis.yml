language:
  - ruby

rvm:
  - 2.6.2

env:
  - "RACK_ENV=travis_ci"

before_install:
  - cp config/database.travis.yml config/database.yml
  - gem update --system
  - gem install bundler
  - bundle install
  - rake db:migrate

services:
  - postgresql

install:
  - bundle exec rspec
  - bundle exec rubocop
